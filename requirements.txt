# ============================================================
# A3 个性化资源生成与学习多智能体系统 — 依赖清单
# 版本已根据开发环境 pip freeze 锁定，确保全新环境可复现
# ============================================================

# ---- Web 框架 ----
fastapi==0.136.0
uvicorn[standard]==0.44.0
python-multipart==0.0.26
pydantic==2.13.2
pydantic-settings==2.13.1

# ---- 前端（HTML/CSS/JS，由 FastAPI StaticFiles 提供）----
# （前端代码在 frontend/ 目录，无需 pip 安装）

# ---- LLM & Agent ----
langchain==1.2.15
langchain-community==0.4.1
langgraph==1.1.8
openai==2.32.0          # 通义千问/讯飞星火等兼容 OpenAI 协议的 API 客户端

# ---- RAG / 向量检索 ----
numpy==2.4.4
pgvector==0.4.2               # PostgreSQL pgvector 扩展 Python 客户端

# ---- 数据库 ----
SQLAlchemy==2.0.49
asyncpg==0.31.0         # PostgreSQL 异步驱动
alembic==1.18.4         # 数据库迁移管理
PyYAML==6.0.3           # YAML 配置文件解析

# ---- 文档解析 ----
pypdf==6.10.2
python-docx==1.2.0
Markdown==3.10.2
pymupdf4llm==1.27.2.3   # PDF → Markdown（保留标题/表格/列表结构）
mammoth==1.12.0         # DOCX → Markdown

# ---- 邮件 ----
aiosmtplib==5.1.1
Jinja2==3.1.6           # 邮件 HTML 模板渲染（backend/email/templates.py）

# ---- 工具 ----
python-dotenv==1.2.2
loguru==0.7.3
tenacity==9.1.4         # 重试装饰器
PyJWT==2.12.1           # JWT 编解码
bcrypt==5.0.0           # 密码哈希（已用于 auth）
jieba==0.42.1

# ---- 测试 ----
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.28.1           # AsyncClient for FastAPI testing
